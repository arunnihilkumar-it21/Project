pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                 echo "Pipeline built"
            }
        }
        stage('Test') { 
            steps {
                 echo "Pipeline test"
            }
        }

    }
}
